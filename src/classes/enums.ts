export enum Direction {
  Left,
  Right,
  Up,
  Down,
}

export enum BounceAngle {
  Right,
  Acute,
  VeryAcute,
  Obtuse,
  VeryObtuse,
  Random,
}

export enum CollisionBehaviour {
  Passthrough,
  Randomnew,
  Reversedirection,
  Onedirection,
  Clump,
  Stick,
}

export enum MovementBehaviour {
  Straight,
  Zigzag,
  Pulse
}
